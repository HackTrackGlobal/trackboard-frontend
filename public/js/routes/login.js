// Login Route
$(document).createRoute({
    path: 'login',
    templateUrl: 'views/login.html',
    controller: function() {

        // Setting the title
        setTitle('Login');

        /**
         * Login to the system
         */

    }
});