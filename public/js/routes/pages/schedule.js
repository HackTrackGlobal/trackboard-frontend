// Main Game Route
$(document).createRoute({
    path: 'schedule',
    templateUrl: 'views/pages/schedule.html',
    controller: function() {

        // Setting the title
        setTitle('Schedule');
    }
});