// Main Game Route
$(document).createRoute({
    path: 'broadcasts',
    templateUrl: 'views/pages/broadcasts.html',
    controller: function() {

        // Setting the title
        setTitle('Broadcasts');
    }
});